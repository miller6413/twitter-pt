<h1>@<%= current_user.username %>'s Twitter Feed</h1>
<p>Here is what your "friends" are saying:</p>


<div>
    <% @following_tweets.reverse.each do |tweet| %>
       <div class="well">
    <p>
      <%= image_tag tweet.user.avatar.url, size: 30  %> 
      <%= link_to tweet.user.name, show_user_path(id: tweet.user_id) %> 
      @<%= link_to tweet.user.username, show_user_path(id: tweet.user_id) %> 
      &bull; <%= time_ago_in_words(tweet.created_at) if Time.now - tweet.created_at < 518400 %> 
<%= tweet.created_at.strftime('%b %e') if Time.now - tweet.created_at > 518400 %>

    </p>
    <p><%= tweet.message.html_safe %></p>
  </div>
    <% end %>
</div>
